'use strict';

// export const host = `http://${window.location.host.replace(/:.*$/, '')}:3000`;
export const host = `http://localhost:3000`;

export const ERR_OK = 200;

export const commonParams = {
  
};

export const rankIds = [0, 1, 3, 7, 8, 14, 15, 17, 18, 20];

export const searchTypes = {
  songs: 1, 
  artists: 100, 
  playlists: 1000
};